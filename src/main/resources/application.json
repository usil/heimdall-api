{
  "nodeboot": {},
  "login": {
    "engine": "default",
    "default": {
      "redirectWebBaseurl": "${HEIMDALL_WEB_BASE_URL}",
      "authorizeUrl": "/v1/sing-in/default"
    },
    "microsoft": {
      "oauth2BaseUrl": "https://login.microsoftonline.com",
      "userDetailsApiEndpoint": "https://graph.microsoft.com/v1.0/me",
      "responseType": "code",
      "responseMode": "query",
      "clientId": "${MICROSOFT_CLIENT_ID}",
      "clientSecret": "${MICROSOFT_CLIENT_SECRET}",
      "redirectUri": "${MICROSOFT_REDIRECT_URL}",
      "tenant": "common",
      "scopes": "offline_access user.read mail.read"
    }
  },
  "database": {
    "host": "${HEIMDALL_DATABASE_HOST}",
    "port": "${HEIMDALL_DATABASE_PORT}",
    "user": "${HEIMDALL_DATABASE_USER}",
    "password": "${HEIMDALL_DATABASE_PASSWORD}",
    "name": "${HEIMDALL_DATABASE_NAME}"
  },
  "oauth2": {
    "jwtSecret": "changeme",
    "jwtExpiration": "3600s",
    "disableRoutes": true,
    "defaultAdminUser": "admin",
    "defaultAdminPassword": "changeme"
  },
  "websites": [
    {
      "url": "https://aj-derteano.github.io",
      "name": "porfolio",
      "description": "Web de portafolio",
      "expected_code": 200,
      "cronExpression": "*/59 * * * * *"
    },
    {
      "url": "test.com",
      "name": "test",
      "description": "Web de prueba",
      "expected_code": 500,
      "cronExpression": "*/59 * * * * *"
    },
    {
      "url": "https://gist.githubusercontent.com",
      "name": "Test 2",
      "description": "Web de prueba",
      "expected_code": 200,
      "cronExpression": "*/59 * * * * *"
    }
  ]
}